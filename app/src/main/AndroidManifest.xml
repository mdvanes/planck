<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="androidx.car.app.ACCESS_SURFACE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature android:name="android.hardware.type.automotive" android:required="true"/>
    <uses-feature android:name="android.software.car.templates_host" android:required="true"/>

    <application
        android:name=".PlanckApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Planck.SplashScreen"
        android:appCategory="audio"
        tools:targetApi="31">

        <meta-data android:name="com.android.automotive.appCategory" android:value="audio"/>

        <activity
            android:name=".MainActivity"
            android:configChanges="uiMode"
            android:exported="true"
            android:theme="@style/Theme.Planck.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.APP_MEDIA" />
            </intent-filter>
            <meta-data android:name="distractionOptimized" android:value="true"/>
        </activity>

        <service
            android:name=".CarMediaService"
            android:exported="true"
            android:permission="androidx.car.app.ACCESS_SURFACE">
            <intent-filter>
                <action android:name="androidx.car.app.CarAppService" />
                <category android:name="androidx.car.app.category.MEDIA" />
            </intent-filter>
            <meta-data android:name="androidx.car.app.minCarApiLevel" android:value="3" />
            <meta-data android:name="androidx.car.app.hosts" android:value="*" />
            <meta-data android:name="androidx.car.app.distraction.optimized" android:value="true" />
            <meta-data android:name="distractionOptimized" android:value="true"/>
        </service>

        <service
            android:name=".ClusterCarAppService"
            android:exported="true"
            android:permission="androidx.car.app.ACCESS_SURFACE">
            <intent-filter>
                <action android:name="androidx.car.app.CarAppService" />
            </intent-filter>
            <meta-data android:name="androidx.car.app.minCarApiLevel" android:value="3" />
            <meta-data android:name="androidx.car.app.hosts" android:value="*" />
            <meta-data android:name="androidx.car.app.distraction.optimized" android:value="true" />
            <meta-data android:name="distractionOptimized" android:value="true"/>
        </service>

        <service
            android:name=".MediaPlaybackService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
            <meta-data android:name="distractionOptimized" android:value="true"/>
        </service>

        <receiver
            android:name="androidx.media.session.MediaButtonReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
